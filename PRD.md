# 微信中文语气键盘 PRD

## 1. 产品概述

### 1.1 产品名称

暂定名：会说话键盘

### 1.2 产品定位

面向中文用户在微信聊天场景中的智能输入法。用户先输入自己的原话，再通过键盘上的 AI 操作，将内容改写成更自然、更得体、更符合关系场景的表达。

一句话定义：

> 写得直，发得体。

### 1.3 核心价值

帮助用户在微信聊天中降低表达成本，尤其是在催促、拒绝、道歉、感谢、客户沟通、职场沟通、亲密关系沟通等场景下，把“想说的话”改成“适合发出去的话”。

产品不以长文写作、翻译或聊天机器人为核心，而是聚焦微信短消息的语气优化。

## 2. 背景与机会

### 2.1 用户痛点

中文微信沟通高度依赖语境、关系和分寸。同一句话发给朋友、老板、客户、对象，表达方式完全不同。用户经常遇到以下问题：

- 原话太直接，担心冒犯。
- 想催别人，但不想显得强势。
- 想拒绝，但不想破坏关系。
- 想表达不满，但不想撕破脸。
- 想跟客户或老板沟通，但不知道如何显得专业。
- AI 工具生成内容太长、太正式、太油腻，不像微信聊天。
- 在微信里复制到外部 AI App 再粘贴回来，流程太重。

### 2.2 产品机会

iOS 第三方键盘可以嵌入系统输入流程，天然适合做轻量文本增强。相比独立 App，本产品的优势是：

- 在微信输入现场直接使用。
- 用户不需要切换 App。
- 可以根据当前短文本快速给出候选。
- 通过关系和场景偏好，输出更符合中文聊天习惯。

### 2.3 关键约束

- iOS 第三方键盘联网需要用户开启 Full Access。
- 键盘必须在不开启 Full Access 时仍保持基础输入能力。
- 涉及用户输入内容和 API Key，隐私信任是产品成败关键。
- 微信输入框替换能力可能受系统限制，需要提供复制兜底。

## 3. 目标用户

### 3.1 核心用户

1. 职场微信高频用户
   - 需要和老板、同事、客户频繁沟通。
   - 关注措辞、礼貌、效率和专业感。

2. 社交关系敏感用户
   - 容易纠结怎么说更合适。
   - 常见场景包括拒绝邀请、解释原因、安慰、道歉、表达不满。

3. 销售、客服、运营类用户
   - 需要高频回复客户。
   - 希望更礼貌、更有耐心、更有转化感。

### 3.2 非目标用户

- 需要长文写作的人。
- 需要完整 AI 聊天助手的人。
- 主要使用英文邮件或专业文档写作的人。
- 不愿开启第三方键盘 Full Access 的用户。

## 4. 产品目标

### 4.1 MVP 目标

验证用户是否愿意在微信输入过程中使用 AI 键盘优化中文短消息。

### 4.2 体验目标

- 用户从输入原文到拿到可发送候选，控制在 3 秒左右。
- 主路径不超过 2 次点击。
- 输出结果要像微信聊天，不像 AI 长文。
- 用户始终知道什么时候会发送文本给模型。

### 4.3 商业目标

第一阶段不做复杂商业化，优先验证留存和高频场景。后续可支持：

- Pro 版高级模板。
- 多模型支持。
- 云端同步用户自定义动作。
- 企业版客户话术模板。

## 5. 核心使用流程

### 5.1 首次使用流程

1. 用户打开 App。
2. 选择主要使用场景：
   - 职场沟通
   - 客户沟通
   - 朋友聊天
   - 亲密关系
   - 家人沟通
3. 设置默认说话风格：
   - 自然
   - 礼貌
   - 直接
   - 松弛
   - 成熟
   - 可爱
4. 添加 LLM API Key。
5. 引导用户开启系统键盘和 Full Access。
6. 进入测试 Playground，试用一次“润色”。

### 5.2 微信内使用流程

1. 用户在微信输入框输入原话。
2. 用户点击键盘顶部 AI 操作，例如“委婉”。
3. 键盘获取可用输入上下文。
4. 如果无法稳定读取，提示用户先复制文本或使用手动输入模式。
5. 调用用户配置的 LLM。
6. 返回 3 个候选结果。
7. 用户选择一个候选。
8. 系统尝试替换原文。
9. 如果替换失败，自动复制候选并提示用户粘贴。

## 6. 功能需求

### 6.1 键盘基础输入

优先级：P0

需求：

- 提供中文用户可接受的基础键盘体验。
- 支持切换到下一个键盘。
- 支持删除、空格、换行、标点。
- 在无 Full Access 时仍可基础输入。

验收标准：

- 用户可以在微信中正常输入文字。
- 系统键盘切换按钮可用。
- 无网络权限时不阻塞基础键盘功能。

### 6.2 AI 工具条

优先级：P0

键盘顶部提供固定工具条：

- 润色
- 委婉
- 高情商
- 简短
- 更多

验收标准：

- 用户能在键盘首屏看到 4 个高频动作。
- 点击动作后进入生成状态。
- 工具条在小屏 iPhone 上不遮挡输入区域。

### 6.3 默认 AI 动作

优先级：P0

MVP 内置 6 个动作：

1. 润色
   - 保持原意，优化表达自然度。
2. 委婉
   - 降低攻击性，让表达更柔和。
3. 高情商
   - 更体面、更照顾对方感受，但不能油腻。
4. 简短
   - 压缩文字，保留核心意思。
5. 催一下
   - 用礼貌但明确的方式提醒对方。
6. 拒绝
   - 帮用户拒绝请求、邀请或安排。

验收标准：

- 每个动作返回 3 个候选。
- 候选均为中文。
- 候选适合微信短消息，避免过长段落。

### 6.4 关系选择

优先级：P0

用户可在键盘中选择消息对象：

- 朋友
- 同事
- 老板
- 客户
- 对象

默认关系由 App 设置中配置，也可以在键盘内临时切换。

验收标准：

- 同一句原文在不同关系下输出风格明显不同。
- 切换关系不需要离开当前微信输入场景。

### 6.5 候选结果面板

优先级：P0

点击 AI 动作后，键盘上方展示结果面板：

- 加载状态
- 3 个候选结果
- 每条候选支持点击使用
- 支持重新生成
- 支持复制

验收标准：

- 加载、成功、失败、无权限状态都有明确反馈。
- 单条候选文字不应挤压到不可读。
- 用户能快速选择并发送。

### 6.6 替换与复制兜底

优先级：P0

由于微信和 iOS 输入法限制，直接替换当前输入可能不稳定，产品必须支持复制兜底。

需求：

- 优先尝试替换原文。
- 替换不可用时，自动复制候选到剪贴板。
- 显示提示：“已复制，粘贴到输入框即可发送”。

验收标准：

- 在无法替换的场景下，用户仍能完成发送。
- 不因替换失败导致结果丢失。

### 6.7 API Key 设置

优先级：P0

用户在 App 本体中配置 API Key。

MVP 支持：

- OpenAI-compatible API。
- 自定义 Endpoint。
- 自定义 Model。

安全要求：

- API Key 存储在 iOS Keychain。
- 键盘扩展只读取必要配置。
- 不默认上传用户输入到产品自有服务器。

验收标准：

- 用户可以新增、测试、删除 API Key。
- Key 不以明文展示。
- 请求失败时给出可理解错误。

### 6.8 用户偏好

优先级：P1

偏好项：

- 默认关系：朋友 / 同事 / 老板 / 客户 / 对象
- 默认风格：自然 / 礼貌 / 直接 / 松弛 / 成熟 / 可爱
- 改写程度：轻微润色 / 明显优化 / 帮我重写
- 输出长度：短一点 / 默认 / 详细一点
- 语气边界：
  - 不要太油
  - 不要太正式
  - 不要 emoji
  - 保留我的说话味道

验收标准：

- 偏好会影响所有默认动作。
- 用户无需理解 prompt 也能完成配置。

### 6.9 敏感内容保护

优先级：P1

需求：

- 检测到疑似密码、验证码、银行卡、身份证号等内容时，提示用户确认。
- 用户可以取消发送给 LLM。
- 隐私页面说明数据流向。

验收标准：

- 高风险文本触发提示。
- 用户明确确认后才继续调用模型。

## 7. 非功能需求

### 7.1 性能

- 首次响应目标：3 秒内。
- 超过 8 秒显示超时提示。
- 支持取消生成。

### 7.2 隐私

- 默认不保存聊天历史。
- 默认不记录完整输入内容。
- 用户可清除本地缓存。
- 明确说明文本会发送到用户配置的模型服务商。

### 7.3 稳定性

- 网络失败时不影响基础键盘输入。
- API Key 失效时提示去 App 修复。
- Full Access 未开启时提示需要开启才能使用 AI 功能。

## 8. LLM 输出策略

### 8.1 输出原则

- 像微信中文短消息。
- 避免长段落。
- 避免 AI 腔。
- 不编造事实。
- 保持用户原意。
- 不替用户承诺未提到的事情。
- 根据关系调整措辞。

### 8.2 输出格式

每次返回 3 个候选：

- 候选 1：稳妥自然。
- 候选 2：更礼貌委婉。
- 候选 3：更简短直接。

### 8.3 示例

输入：

> 你怎么还没发给我

关系：同事  
动作：催一下

输出：

1. 我想问下这个方便什么时候发我呀？我这边好提前安排一下。
2. 辛苦啦，我这边可能快要用到了，方便的话麻烦你晚点发我一下。
3. 这个今天方便发我吗？我这边后面需要用到。

## 9. MVP 范围

### 9.1 包含

- iOS App 本体。
- iOS Keyboard Extension。
- API Key 本地配置。
- OpenAI-compatible 调用。
- 6 个默认动作。
- 5 种关系选择。
- 3 个候选结果。
- 复制兜底。
- Full Access 引导。
- 基础隐私说明。

### 9.2 不包含

- 账号系统。
- 云同步。
- 付费系统。
- 多端同步。
- Prompt 市场。
- 长文写作编辑器。
- 企业团队管理。

## 10. 成功指标

### 10.1 激活指标

- 完成键盘开启率。
- 完成 API Key 配置率。
- 首次成功生成率。

### 10.2 使用指标

- DAU。
- 每用户每日 AI 动作次数。
- 候选点击率。
- 复制兜底使用率。
- 生成失败率。

### 10.3 留存指标

- D1 留存。
- D7 留存。
- 每周活跃天数。

### 10.4 质量指标

- 用户手动重新生成率。
- 用户删除候选后重新输入比例。
- “太 AI / 太油 / 太长”的负反馈率。

## 11. 主要风险

1. iOS 键盘限制导致微信内替换体验不稳定。
2. 用户不愿开启 Full Access。
3. 用户不理解为什么需要 API Key。
4. LLM 输出过于正式或油腻。
5. 网络延迟影响聊天流畅度。
6. 隐私说明不足导致信任问题。

## 12. 版本规划

### V0.1 Prototype

- App 设置页。
- 键盘工具条。
- 固定 Prompt。
- 生成 3 个候选。
- 手动复制。

### V0.2 MVP

- API Key 管理。
- Full Access 引导。
- 关系选择。
- 6 个默认动作。
- 替换与复制兜底。

### V0.3 Beta

- 用户偏好。
- 敏感内容保护。
- 自定义动作。
- 请求错误诊断。

### V1.0

- 稳定上架版本。
- 完整隐私说明。
- 可用性优化。
- 基础付费策略验证。
