# 微信中文语气键盘 UI 设计

## 1. 设计目标

本产品的 UI 应该像一个轻巧的微信表达辅助层，而不是完整 AI 聊天工具。设计重点：

- 快：用户正在聊天，不能被复杂界面打断。
- 稳：文字、状态和按钮都要清楚，不制造焦虑。
- 轻：键盘空间有限，减少大面积卡片和复杂控件。
- 像中文聊天：输出内容短、自然、有分寸。
- 可控：用户能看见候选，确认后再使用。

## 2. 视觉风格

### 2.1 整体气质

关键词：

- 干净
- 轻量
- 温和
- 实用
- 不卖弄 AI 感

避免：

- 大面积紫蓝渐变
- 科技感过强的霓虹色
- 过度拟物
- 大面积营销文案
- 卡片套卡片

### 2.2 色彩建议

主色：

- 微信绿色方向，但不要完全复制微信。
- 建议：`#16A34A` 或 `#22C55E`

辅助色：

- 文本主色：`#111827`
- 次级文本：`#6B7280`
- 分割线：`#E5E7EB`
- 背景：`#F9FAFB`
- 键盘键帽：`#FFFFFF`
- 警示：`#F97316`
- 错误：`#DC2626`

### 2.3 字体层级

iOS 使用系统字体 San Francisco。

- App 页面标题：28pt / Semibold
- 分组标题：17pt / Semibold
- 正文：15-16pt / Regular
- 辅助说明：13pt / Regular
- 键盘按钮：14pt / Medium
- 候选文本：15pt / Regular

键盘中不要使用过大的标题，避免压缩可操作区域。

## 3. App 本体信息架构

底部 Tab 建议 4 个：

1. 首页
2. 偏好
3. 动作
4. 隐私

### 3.1 首页

目标：

- 告诉用户当前是否可用。
- 引导完成必要设置。
- 提供测试入口。

布局：

```text
┌─────────────────────────┐
│ 会说话键盘              │
│ 微信里的中文表达助手    │
├─────────────────────────┤
│ 状态                    │
│ API Key       已配置     │
│ 键盘权限      未开启     │
│ Full Access   未开启     │
├─────────────────────────┤
│ [去开启键盘]             │
│ [测试一句]               │
├─────────────────────────┤
│ 常用场景                 │
│ 催一下  委婉  拒绝       │
└─────────────────────────┘
```

设计说明：

- 状态模块使用列表式，不用复杂仪表盘。
- 未完成项用明确按钮引导。
- 首页不放长篇隐私说明，只放入口。

### 3.2 API Key 设置页

入口：首页状态卡片或设置流程。

布局：

```text
┌─────────────────────────┐
│ API 设置                │
├─────────────────────────┤
│ 服务商                  │
│ OpenAI Compatible   >   │
├─────────────────────────┤
│ Endpoint                │
│ https://api.openai...   │
├─────────────────────────┤
│ Model                   │
│ gpt-4.1-mini            │
├─────────────────────────┤
│ API Key                 │
│ sk-••••••••••••••       │
├─────────────────────────┤
│ [测试连接]               │
│ [保存]                  │
└─────────────────────────┘
```

交互：

- API Key 默认隐藏。
- 支持粘贴。
- 测试连接成功后显示绿色状态。
- 失败时展示可理解错误，例如“Key 无效”“Endpoint 无法访问”“模型不可用”。

### 3.3 偏好页

目标：用非 prompt 的方式让用户设置输出风格。

布局：

```text
┌─────────────────────────┐
│ 偏好                    │
├─────────────────────────┤
│ 默认发给谁              │
│ 朋友 同事 老板 客户 对象 │
├─────────────────────────┤
│ 我的说话风格            │
│ 自然 礼貌 直接 松弛 成熟 │
├─────────────────────────┤
│ 改写程度                │
│ 轻微润色 明显优化 重写   │
├─────────────────────────┤
│ 输出习惯                │
│ [x] 保留我的说话味道     │
│ [x] 不要太油            │
│ [ ] 不要 emoji          │
│ [ ] 不要太正式          │
└─────────────────────────┘
```

控件：

- 关系和风格用 segmented control 或横向 chip。
- 边界设置用 checkbox / toggle。
- 不提供大段 prompt 输入作为主入口。

### 3.4 动作页

目标：展示内置动作，后续支持自定义。

布局：

```text
┌─────────────────────────┐
│ 动作                    │
├─────────────────────────┤
│ 固定在键盘顶部          │
│ 润色  委婉  高情商  简短 │
├─────────────────────────┤
│ 更多动作                │
│ 催一下                  │
│ 拒绝                    │
│ 道歉                    │
│ 感谢                    │
│ 安慰                    │
└─────────────────────────┘
```

交互：

- MVP 阶段可以只允许排序，不允许编辑 prompt。
- Beta 阶段增加自定义动作。

### 3.5 隐私页

目标：建立信任。

内容应直接、具体：

```text
┌─────────────────────────┐
│ 隐私                    │
├─────────────────────────┤
│ AI 什么时候工作？        │
│ 只有当你点击润色、委婉等 │
│ 操作时，才会处理文本。   │
├─────────────────────────┤
│ 文本发到哪里？           │
│ 发送到你配置的模型服务商。│
├─────────────────────────┤
│ API Key 存在哪里？       │
│ 保存在本机 Keychain。    │
├─────────────────────────┤
│ [清除本地缓存]           │
└─────────────────────────┘
```

避免模糊承诺，例如“绝对安全”。说明真实数据流。

## 4. Onboarding 设计

### 4.1 第 1 屏：价值说明

```text
写得直，发得体

在微信里把原话改得更自然、更委婉、更适合发出去。

[开始设置]
```

### 4.2 第 2 屏：选择主要场景

```text
你主要用它来处理什么？

[职场沟通]
[客户沟通]
[朋友聊天]
[亲密关系]
[家人沟通]
```

### 4.3 第 3 屏：选择说话风格

```text
希望它更像怎么说话？

[自然]
[礼貌]
[松弛]
[成熟]
[直接]
[可爱]
```

### 4.4 第 4 屏：配置 API

```text
添加你的模型 API Key

你的 Key 会保存在本机，用于调用你选择的模型服务。

[填写 API Key]
[稍后设置]
```

### 4.5 第 5 屏：开启键盘

```text
开启 iOS 键盘权限

1. 打开系统设置
2. 添加“会说话键盘”
3. 开启 Full Access 以使用 AI 功能

[打开设置指引]
```

## 5. 键盘 UI 设计

### 5.1 默认状态

```text
┌─────────────────────────┐
│ 发给：同事 ▾            │
│ 润色 委婉 高情商 简短 更多│
├─────────────────────────┤
│ q  w  e  r  t  y ...    │
│ a  s  d  f  g  h ...    │
│ ⇧  z  x  c  v  b ...⌫  │
│ 🌐 123     空格      回车│
└─────────────────────────┘
```

设计说明：

- 第一行显示当前关系。
- 第二行是 AI 动作工具条。
- 工具条按钮高度控制在 32-36pt。
- “更多”打开动作面板。

### 5.2 生成中状态

```text
┌─────────────────────────┐
│ 正在优化...        取消  │
├─────────────────────────┤
│ q  w  e  r  t  y ...    │
└─────────────────────────┘
```

说明：

- 不要全屏 loading。
- 保留键盘基础输入能力。
- 支持取消。

### 5.3 候选结果状态

```text
┌─────────────────────────┐
│ 选择一个发出去           │
├─────────────────────────┤
│ 我想问下这个方便什么时候 │
│ 发我呀？我这边好提前安排 │
│ 一下。                  │
├─────────────────────────┤
│ 辛苦啦，我这边可能快要用 │
│ 到了，方便的话麻烦晚点发 │
│ 我一下。                │
├─────────────────────────┤
│ 这个今天方便发我吗？我这 │
│ 边后面需要用到。         │
├─────────────────────────┤
│ 重新生成          复制   │
└─────────────────────────┘
```

交互：

- 点击候选即使用。
- 长按候选复制。
- 底部提供重新生成和复制。
- 如果替换失败，提示已复制。

### 5.4 更多动作面板

```text
┌─────────────────────────┐
│ 更多                    │
├─────────────────────────┤
│ 催一下    拒绝    道歉   │
│ 感谢      安慰    夸人   │
│ 求帮忙    提意见  结束聊天│
└─────────────────────────┘
```

说明：

- 用网格按钮。
- 每个按钮短词，不加解释文案。
- 后续可允许用户编辑排序。

### 5.5 关系选择面板

```text
┌─────────────────────────┐
│ 这句话发给谁？           │
├─────────────────────────┤
│ 朋友                    │
│ 同事                    │
│ 老板                    │
│ 客户                    │
│ 对象                    │
└─────────────────────────┘
```

交互：

- 点击顶部“发给：同事”打开。
- 选择后立即回到键盘。
- 仅影响当前会话或本次生成，是否设为默认可在 App 偏好中配置。

## 6. 关键状态设计

### 6.1 Full Access 未开启

```text
需要开启 Full Access 才能使用 AI

基础输入仍可使用。AI 功能需要联网调用你配置的模型。

[查看开启方法]
```

### 6.2 API Key 未配置

```text
还没有配置 API Key

请打开 App 添加你的模型 API Key。

[打开 App]
```

### 6.3 生成失败

```text
这次没有生成成功

可能是网络、Key 或模型配置问题。

[重试] [复制原文]
```

### 6.4 敏感内容提醒

```text
这段内容可能包含敏感信息

点击继续后，会发送到你配置的模型服务商。

[取消] [继续]
```

## 7. 文案规范

### 7.1 按钮文案

使用短词：

- 润色
- 委婉
- 高情商
- 简短
- 催一下
- 拒绝
- 复制
- 重试
- 取消

避免：

- 立即启动智能优化
- 使用 AI 帮我改写
- 生成高级表达

### 7.2 结果文案风格

输出应符合：

- 短句优先。
- 不堆叠客套话。
- 不使用过多感叹号。
- 不默认加 emoji。
- 不使用明显 AI 套话。

应避免的表达：

- “希望你能理解”
- “感谢你的支持与配合”
- “我非常重视我们的关系”
- “在这个问题上，我认为我们可以进行更深入的沟通”

## 8. 典型界面流程

### 8.1 催同事发资料

用户输入：

> 你怎么还没发给我

操作：

1. 关系选择：同事。
2. 点击“催一下”。
3. 显示 3 个候选。
4. 用户选择候选 1。
5. 替换失败则复制。

候选：

1. 我想问下这个方便什么时候发我呀？我这边好提前安排一下。
2. 辛苦啦，我这边可能快要用到了，方便的话麻烦你晚点发我一下。
3. 这个今天方便发我吗？我这边后面需要用到。

### 8.2 拒绝朋友邀请

用户输入：

> 我不想去

操作：

1. 关系选择：朋友。
2. 点击“拒绝”。

候选：

1. 我这次就先不去啦，你们玩得开心点。
2. 今天我可能不太想出门，下次有机会再一起。
3. 这次我就不参加啦，改天再约。

### 8.3 对客户委婉说明延期

用户输入：

> 今天弄不完

操作：

1. 关系选择：客户。
2. 点击“委婉”。

候选：

1. 这部分今天可能还需要再确认一下，我会尽快整理好后发您。
2. 今天这边可能无法完全处理完，我会优先推进，完成后第一时间同步您。
3. 这个今天可能来不及全部完成，我先把已有进展同步给您。

## 9. 组件清单

### 9.1 App 组件

- SetupStatusList
- ProviderPicker
- APIKeyField
- ModelField
- PreferenceSegment
- PreferenceToggle
- ActionList
- PrivacyInfoBlock
- TestPlayground

### 9.2 Keyboard 组件

- KeyboardToolbar
- RecipientSelector
- ActionButton
- MoreActionGrid
- GenerationStatusBar
- CandidatePanel
- CandidateItem
- ErrorBanner
- SensitiveContentAlert

## 10. MVP 原型页面清单

需要先画这些页面：

1. Onboarding 价值页。
2. 场景选择页。
3. API Key 设置页。
4. 键盘开启引导页。
5. App 首页。
6. 偏好页。
7. 键盘默认状态。
8. 键盘生成中状态。
9. 键盘候选状态。
10. 更多动作面板。
11. 关系选择面板。
12. 错误状态。

## 11. 设计验收标准

- 小屏 iPhone 上键盘工具条不拥挤。
- 候选结果至少能完整展示 2 条，第三条可滚动查看。
- 所有主操作单手可点。
- 用户能在 2 次点击内完成一次优化。
- 无 Full Access、无 API Key、网络失败均有明确状态。
- 文案不制造隐私误导。
- 视觉上像实用工具，不像营销页。
